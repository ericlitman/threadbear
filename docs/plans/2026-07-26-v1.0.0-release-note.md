# v1.0.0 release evidence — 2026-07-26

Tag `v1.0.0` = `e103514`. Every runbook step below was executed against published
artifacts, with the command's actual output as the evidence standard.

## Release gate (Verification Contract, as amended 2026-07-26)

Five-run Luna-medium series over the operator-maintained 114-case corpus
(`24e4761d…`, threadbear-eval `main`), one fresh App Server per run, harness commit
`1b49422`. Scored under the operator-approved amendments (four-of-five systematic
threshold; certified accepted sets for `019f717e` and `019f72b3`):

- **Systematic dangerous cases: 0. Unscoreable cases: 0. Verdict: PASS.**
- Per-run correct 90/90/91/90/92 of 114; false_complete 0/0/0/1/0; false_next_steps
  1/0/1/0/1; unscored 0 across all runs.
- Flap (reported, non-gating): `019f6cf7` (1 run), `019f76b6` (2), `019f85d5` (1).
- Caveat carried per BEAR-28: cases are classified one per call; production packs
  batches (KTD3). The gate is a floor, not a production guarantee.
- The verdict is a deterministic re-score of the recorded series under the
  accepted-sets amendment; sampled classifications unchanged. Artifacts:
  `gate-final2.log`, `gate-final2-rescored.json` (studio2 `/tmp/tbeval/`, copies with
  the operator). The next scheduled gate run validates the amended scoring live.
- Gate-relevant delta between the gate commit and the tag: PR #28 (token figures —
  no classifier/packing/footer paths), PR #30 (dead `TitleMessage` plumbing removal —
  parsing and resolution outputs unchanged, verified by diff), PRs #27/#29 (the gate
  amendments themselves).

## Secret and corpus scans (BEAR-35)

gitleaks full history: `ericlitman/threadbear` 81 commits and
`ericlitman/threadbear-web` 19 commits — no leaks. Corpus cross-reference: 120 private
thread ids + 154 content probes against every commit of both public repos — zero hits;
no UUIDv7-shaped identifiers at either HEAD.

## Published artifacts

`latest.json` version `1.0.0`; both binaries downloaded via the manifest's own URLs:

- `threadbear_darwin_arm64` sha256 `87adcd5b3c681ba0…` — matches published `.sha256`.
- `threadbear_darwin_amd64` sha256 `cf7768a44ef1f0a8…` — matches published `.sha256`;
  Mach-O x86_64 confirmed; **never executed on Intel hardware** (BEAR-37: operator
  ruled non-gating; verify when hardware arrives, before any public announcement).
- arm64 `version --json`: `{"installed_version":"1.0.0","website":"https://threadbear.sh"}`.

## Published-one-liner smoke (BEAR-34 minimal, temp home, arm64)

`curl -fsSL https://threadbear.sh/install.sh | sh -s -- --noninteractive --confirm …`
against the live site (serving the canonical installer, byte-identical to the tag's):

- Preview → install: all seven managed resources created; control task
  `019fa0a7-c9ff…` created in the isolated `CODEX_HOME`; `migrated=false`.
- `threadbear version`: 1.0.0. `threadbear self-test`: all checks ok.
- LaunchAgent `org.litman.threadbear` loaded and running in the gui domain.
- **Uninstall failed on first invocation while a heartbeat was executing and
  succeeded identically on retry** (`changed=true`, state deleted) — filed as
  BEAR-44 with the mechanism hypothesis. launchd pre/post state captured; both
  labels absent after cleanup, matching pre-state.

## Site

`threadbear.sh/install.sh` serves the canonical installer (sha `4597e98c…`);
`threadbear.dev` and `threadbare.sh` 301 to the canonical host. `installer.lock.json`
pinned to the immutable `v1.0.0` tag (threadbear-web PR #6).

## Known caveats shipped with v1.0.0

- Intel binary cross-compiled and checksum-verified, not hardware-executed (BEAR-37).
- Uninstall-during-heartbeat race, retry succeeds (BEAR-44).
- Gate floor caveat (BEAR-28); no-TTY installer guidance pending (BEAR-33).
- `update` does not yet refresh managed AGENTS/skill blocks — required before the
  first post-v1 release (BEAR-27).
